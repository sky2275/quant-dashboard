# 2026-08-20 工作日志

## 量化工作台盘前数据刷新 + 两个缺陷修复（commit 3ab8ce7，已 push main）

### 数据刷新
- `scripts/feed.py` → cache/market_snapshot.json，updated_at=2026-08-20 09:05:50
- westock-mcp `data_sector(ranking, sw1, 30)` → cache/sector_raw_westock.json（经 scripts/_build_sector_raw.py 转写）→ `scripts/refresh_sector_data.py` → cache/sector_leader_data.json，updated_at=2026-08-20T09:06:58
- `scripts/build_dashboard.py` → index.html（4.85MB）

### 缺陷 1：cache/holdings.json JSON 损坏（已修）
自 commit 870f62f（8/19 22:30 同步 8/19 交割单）起，文件第 2418 字符多出一个 `}` 提前闭合根对象，
导致尾部 `, "summary": {...}}` 成为「Extra data」非法内容。`json.load` 直接抛错。
- 修法：`s[:2418] + s[2419:]` 重组后解析通过，7 只持仓 + summary 完整，**未改动任何业务数据**。
- 成因：手工/Agent 写入时括号错位。全项目脚本均不产出该 `summary` 块（已 grep 确认），故非脚本级复发 bug。
- 损坏版本仍可从 git 870f62f 取回，未额外留 .bak。

### 缺陷 2：holdings.json → 看板持仓断链（已修）
`build_dashboard.py` L5714 只读 `holdings_cache["positions"]`（扁平列表），
但 cache/holdings.json 实际结构是 `accounts{broker: [...]}` 分账户嵌套，**没有 positions 键** →
`broker_positions` 恒为 `[]` → `_unified_positions()` 回退到 strategy.yaml（其 holdings 已废弃为空）→
持仓 section / 持仓风险 / 持仓任务 / 持仓弹窗 全部取空。
- 修法：在 L5714 call site 增加 accounts→positions 展平分支，仅在 `positions` 缺失时触发（加法式，零回归风险）。
  accounts 条目字段（code/name/account/quantity/avg_cost/pnl）与 `_unified_positions` 期望完全一致，直接展平即可。
- 验证：构建日志出现 `[info] holdings.json 使用 accounts 结构，已展平 7 条持仓`，
  index.html 4835335→4850861 字节，三券商标签（银河证券/东方财富证券/中信建投）均正常渲染。

### 排查笔记（避免以后误改）
- `_paper_trade_card()`（L4626）里的「持仓数 0」读的是**模拟盘**缓存，属模拟账户无仓位，与实盘无关，不是缺陷。

### 未完成项
- mx-ds-mcp 连接器 disconnected（连续第 2 个交易日），步骤 3 跳过：
  macro_commodity / a_news_summary / global_news_summary（updated_at=2026-08-18）、
  sector_contrib_mx（asof=2026-08-14）均保留原值。
  **刻意未把 updated_at 改成当日**——否则会把过期数据伪装成新鲜数据。需用户重连后补刷。

### 盘面
半导体 -7.57%（主力净流出 355.5 亿）、通信设备 -8.66%（-255.8 亿）、元件 -9.04%（-168.3 亿），
科技三兄弟合计净流出近 780 亿；资金避险流入焦炭Ⅱ(+7.56%)、航运港口(+1.11%)与银行三系（国有大行/股份制/城商行）。
持仓侧 8/19 已大幅调仓：通富微电 600→300、君正(东财) 1300→1000、国瓷材料 1200→500(割肉)、
风华高科 1500→500(割肉)、北大荒 500→2000(加仓)，当日 -50,841 元。
