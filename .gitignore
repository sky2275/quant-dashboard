# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# 本地配置文件
config_local.py

# IDE
.vscode/
.idea/

# 临时文件
*.log
*.tmp

# 备份文件
index_*.html

# 券商交割单原始文件（含隐私：真实交易记录/金额/日期，仅留本机，绝不提交）
data/statements/**/*.xlsx
data/statements/**/*.xls
data/statements/**/*.csv
data/statements/**/*.XLS
data/statements/**/*.XLSX
data/statements/**/*.CSV

# 历史逐笔买卖点聚合（同等级隐私：真实成交日期/价格/数量，仅留本机，绝不提交公开仓库）
cache/trades_history.json
cache/trades_factor_backtest.json

# 每日因子纪律检查结果（含持仓数量/成本价/账户，隐私，仅留本机）
cache/factor_discipline.json

# 微信推送凭证（含 Server酱 SendKey / 企业微信 webhook，绝不能提交公开仓库）
config/notify.json

# 建仓触达检测结果 + 去重状态（含现价/触达信号，仅留本机）
cache/entry_plan_alert.json
cache/entry_plan_alert_state.json
