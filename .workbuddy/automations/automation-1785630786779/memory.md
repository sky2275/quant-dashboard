# 量化看板 14:30 市场情绪扫描 — 执行记录

## 2026-08-02 (周日) 14:25
- 触发包装器 `refresh_dashboard.sh`，退出码 0。
- 日志：`skip: non-trade day (14:25)`。
- 结论：周日非交易日，14:25 在 14:30 窗口容差内，本应触发 `feed + scan_a_shares --mode 1430`，但交易日校验先于窗口判断生效，安全跳过。未拉取数据、未重建 HTML、未改动任何文件。符合预期。

## 2026-08-05 (周三) 14:25 → 14:28
- 触发包装器，退出码 0；后台刷新 pid 52996 完成，`[run] DONE`。
- 交易日校验通过（20260805 周三），命中 14:30 窗口，执行 `feed + scan_a_shares --mode 1430`。
- 扫描结果：获取 5889 只股票行情，筛选出 15 只优选股票；`index.html` 已重建（14:28:18，约 1.78 MB），`cache/scan_1430.json` 等已更新。
- 非致命告警：个别概念板块（IT服务/军工装备/文化传媒/军工电子）akshare 成分股为空；deploy 阶段 git `rebase failed`（未提交改动，历史已知），均不影响本地看板重建。
- 结论：成功。未改动任何看板代码/配置/数据文件。

## 2026-08-07 (周五) 14:25 → 14:28
- 触发包装器，退出码 0；后台刷新 pid 80234 于 14:28:37 完成，`[run] DONE`。
- 交易日校验通过（20260807 周五），命中 14:30 窗口，执行 `feed + scan_a_shares --mode 1430`。
- **A股扫描失败**：`ak.stock_zh_a_spot_em()` 东方财富实时行情接口连接被远端关闭，抛 `ConnectionError: RemoteDisconnected`，重试后仍失败（`scan_a_shares.py:161 _load_spot`）。`cache/scan_1430.json` 仍为 8/5 的旧数据，本轮未刷新。
- `feed` 步骤成功：`market_snapshot.json`(14:26)、`backtest_klines.json`(14:27，41只) 已更新；`index.html` 于 14:28 重建（约 3.07 MB，git 提交 3 文件）。
- 同源历史失败：今日 10:22、11:58 两次 scan 同样因东方财富 spot 接口连接中断失败（同一处 `_load_spot`）。
- deploy 阶段 git `rebase failed`（未提交改动，历史已知），不影响本地看板。
- 结论：流程跑通但扫描降级——看板已重建，但 14:30 A股优选扫描为 8/5 旧数据；feed/快照/回测K线为今日新数据。未改动任何看板代码/配置/数据文件，未新建副本、未写入腾讯文档。

## 2026-08-08 (周六) 14:25
- 触发包装器，退出码 0。
- 日志：`skip: non-trade day (14:25)`（14:25:28）。
- 结论：周六非交易日，交易日校验先于窗口判断生效，安全跳过。未拉取数据、未重建 HTML、未改动任何文件、未写入腾讯文档。与 2026-08-02 周日同属非交易日跳过，符合预期。
